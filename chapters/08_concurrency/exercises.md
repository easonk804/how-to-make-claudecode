# 第08章练习题：并发与后台任务

## 基础题
- 在 `v2_background_task.py` 中为后台任务增加进度通知与状态查询接口。

## 进阶题
- 在 `v3_parallel_execution.py` 中支持任务超时取消与超时结果汇总。

## 挑战题
- 实现一个最小任务依赖图（A 完成后 B 才能执行）。

## 可选进阶（v3）
1. 增加 `max_concurrency` 控制并比较性能变化。
2. 设计部分失败重试策略（仅重跑失败任务）。
3. 增加测试：验证并发模式下结果顺序与超时行为可预测。
